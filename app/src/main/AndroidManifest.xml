<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dgp.conejonegro">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Museo"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Error">
        </activity>
        <activity android:name=".lector">
        </activity>

        <activity android:name=".verElemento">
        </activity>

        <activity android:name=".Configuracion">
        </activity>

        <activity android:name=".verElementosRuta">
        </activity>

        <activity android:name=".Principal">
        </activity>

        <activity android:name=".verSalas"
                android:windowSoftInputMode="adjustPan">
        </activity>

        <activity android:name=".verContenidoSala"
            android:windowSoftInputMode="adjustPan">
        </activity>

        <activity android:name=".verRutas"
            android:windowSoftInputMode="adjustPan">
        </activity>

        <activity android:name=".buscador">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>

    </application>

</manifest>